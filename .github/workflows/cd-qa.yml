name: CD QA (Placeholder)

on:
  pull_request:
    types: [opened, reopened, synchronize]
    branches: [main]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "QA deploy — ativar via bootstrap-deploy após Validation Report"
